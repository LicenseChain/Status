{
  "name": "@licensechain/status",
  "version": "1.0.0",
  "private": true,
  "description": "LicenseChain Status Page",
  "scripts": {
    "dev": "next dev",
    "build": "prisma generate && next build",
    "start": "next start",
    "lint": "next lint",
    "typecheck": "tsc --noEmit",
    "deploy": "npm run build && echo Build complete! Deploy the 'out' directory to your hosting provider.",
    "deploy:vercel": "npm run build && vercel --prod",
    "prisma:generate": "prisma generate",
    "postinstall": "prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "clsx": "^2.0.0",
    "lucide-react": "^0.263.1",
    "next": "^14.2.35",
    "next-intl": "^4.7.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "tailwind-merge": "^2.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "autoprefixer": "^10.4.0",
    "eslint": "^8.0.0",
    "eslint-config-next": "^14.2.35",
    "postcss": "^8.4.0",
    "prisma": "^5.22.0",
    "tailwindcss": "^3.3.0",
    "typescript": "^5.3.3"
  },
  "overrides": {
    "glob": "^11.0.0"
  }
}
