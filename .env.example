# LicenseChain Status Page - Environment Variables

# Database connection (PostgreSQL)
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE"

# Crypto Payment Processing Webhook Health Check URL
# This endpoint should return a 200 status when the crypto payment webhook service is operational
CRYPTO_WEBHOOK_HEALTH_URL=https://api.licensechain.app/webhooks/health

# Stripe Payment Processing Webhook Health Check URL
# This endpoint should return a 200 status when the Stripe webhook service is operational
STRIPE_WEBHOOK_HEALTH_URL=https://api.licensechain.app/webhooks/stripe/health

# Public environment variables (accessible in browser)
# These can be used for client-side configuration if needed
NEXT_PUBLIC_WEBHOOK_HEALTH_URL=https://api.licensechain.app/webhooks/health
NEXT_PUBLIC_STRIPE_WEBHOOK_HEALTH_URL=https://api.licensechain.app/webhooks/stripe/health

# Optional: API Keys or other sensitive configuration
# STRIPE_API_KEY=sk_live_...
# CRYPTO_API_KEY=...