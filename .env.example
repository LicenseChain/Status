# ==============================================================================
# LicenseChain Status - Environment Variables
# ==============================================================================
# 
# SECURITY WARNING:
# - Variables marked with [SENSITIVE] contain secrets and MUST be kept private
# - Never commit your actual .env file to version control
# - Use strong, random values for all [SENSITIVE] variables
# - Rotate secrets regularly, especially after any potential exposure
#
# Copy this file to .env and fill in your actual values
# ==============================================================================

# ==============================================================================
# Database Configuration
# ==============================================================================
# [SENSITIVE] Database connection string with credentials
# Format: postgresql://username:password@host:port/database?pgbouncer=true
# For Supabase: Use the connection pooler URL with pgbouncer=true parameter
DATABASE_URL="your-database-url-here"

# ==============================================================================
# Payment Configuration (Optional)
# ==============================================================================
# Required only if you want to enable Fiat and Crypt hook integration

# [SENSITIVE] Crypto Payment Processing Webhook Health Check URL
# This endpoint should return a 200 status when the crypto payment webhook service is operational
CRYPTO_WEBHOOK_HEALTH_URL=your-crypto-webhook-health-here

# [SENSITIVE] Stripe Payment Processing Webhook Health Check URL
# This endpoint should return a 200 status when the Stripe webhook service is operational
STRIPE_WEBHOOK_HEALTH_URL=your-stripe-webhook-health-here

# [PUBLIC] These can be used for client-side configuration if needed
NEXT_PUBLIC_WEBHOOK_HEALTH_URL=your-crypto-webhook-health-here
NEXT_PUBLIC_STRIPE_WEBHOOK_HEALTH_URL=your-stripe-webhook-health-here

# Optional: API Keys or other sensitive configuration
# STRIPE_API_KEY=sk_live_...
# CRYPTO_API_KEY=...